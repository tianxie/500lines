[project]
name = ""
version = ""
description = ""
authors = [
    {name = "tianxie", email = "txie@live.cn"},
]
dependencies = [
    "pytest>=7.1.2",
]
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pdm]

[build-system]
requires = ["pdm-pep517>=0.12.0"]
build-backend = "pdm.pep517.api"
